<div class="blog-post">  

  <h1>Editing <%= @blog.title %></h1>

  <%= render 'form', blog: @blog if logged_in?(:admin) %>

  <%= link_to 'Show', @blog %> |
  <%= link_to 'Back', blogs_path %>
</div>
